# Final update prompt

Use this prompt only after the official nine-run benchmark and canonical protected-test aggregate are complete:

> Finalize the DMS-Eval IEEE manuscript from the one canonical `dms_eval_aggregate` artifact. Do not rerun training or inference, do not reopen protected-test images, do not select runs, and do not use backup, partial, mock, or hand-edited results. First run the repository postprocessor with `--aggregate auto --build`; require the exact 3-model by 3-seed matrix (13, 37, 73), one suite ID, unique source artifacts, both FLOP estimators, all four classes, finite values, and recomputed means plus sample SD (`n=3`). Replace every manuscript `TO_BE_UPDATED` value with validated evidence. Enter the manifest's human judgments through `manuscript/manual_results.json`: training durations from official logs, the best accuracy-efficiency trade-off, and its discussion sentence; assemble the optional qualitative montage only from the evaluator's predeclared seed-13 outputs. Rerun the postprocessor after those inputs. Never invent a number or ranking. Audit the source tree and extracted PDF for unresolved placeholders and mock watermarks, rebuild the IEEE PDF, visually inspect every page, and report the final aggregate path/hash, generated artifacts, remaining placeholder count, page count, build warnings, and any integrity concern.
